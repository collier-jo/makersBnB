# feature "user can't create listing without being signed in" do
#   scenario "user sees error message if they attempt to create listing without being signed in" do
#     visit('/')
#     click_button "Add Listing"
#     expect(page).to have_content "You must be signed in before adding a new listing"
#   end
# end
